package com.zack.xjht.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zack.xjht.entity.GunStateBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "GUN_STATE_BEAN".
*/
public class GunStateBeanDao extends AbstractDao<GunStateBean, Long> {

    public static final String TABLENAME = "GUN_STATE_BEAN";

    /**
     * Properties of entity GunStateBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Position = new Property(1, int.class, "position", false, "POSITION");
        public final static Property GunState = new Property(2, int.class, "gunState", false, "GUN_STATE");
        public final static Property LockState = new Property(3, int.class, "lockState", false, "LOCK_STATE");
    }


    public GunStateBeanDao(DaoConfig config) {
        super(config);
    }
    
    public GunStateBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"GUN_STATE_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"POSITION\" INTEGER NOT NULL ," + // 1: position
                "\"GUN_STATE\" INTEGER NOT NULL ," + // 2: gunState
                "\"LOCK_STATE\" INTEGER NOT NULL );"); // 3: lockState
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"GUN_STATE_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GunStateBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPosition());
        stmt.bindLong(3, entity.getGunState());
        stmt.bindLong(4, entity.getLockState());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GunStateBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPosition());
        stmt.bindLong(3, entity.getGunState());
        stmt.bindLong(4, entity.getLockState());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public GunStateBean readEntity(Cursor cursor, int offset) {
        GunStateBean entity = new GunStateBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // position
            cursor.getInt(offset + 2), // gunState
            cursor.getInt(offset + 3) // lockState
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, GunStateBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPosition(cursor.getInt(offset + 1));
        entity.setGunState(cursor.getInt(offset + 2));
        entity.setLockState(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(GunStateBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(GunStateBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(GunStateBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
